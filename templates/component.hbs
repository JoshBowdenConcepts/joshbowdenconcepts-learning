import { Styled{{pascalCase component}} } from './{{pascalCase component}}.styles'
import { {{pascalCase component}}Props } from './{{pascalCase component}}.types'

export const {{pascalCase component}} = ({ ...props }: {{pascalCase component}}Props) => {
	return <Styled{{pascalCase component}} { ...props }></Styled{{pascalCase component}}>
}