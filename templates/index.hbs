import { {{ pascalCase component }} } from './{{ pascalCase component }}.component';
import { {{ pascalCase component }} }Props from './{{ pascalCase component }}.types';

export {
	{{ pascalCase component }},
	{{ pascalCase component }}Props
}